apiVersion: batch/v1
kind: CronJob
metadata:
  name: top_plays_cron-production
spec:
  schedule: "*/10 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: top_plays_cron-production
            image: osuakatsuki/top_plays_cron:latest
            imagePullPolicy: Always
            command:
              - ./scripts/bootstrap.sh
            env:
              - name: KUBERNETES
                value: 'true'
              - name: PULL_SECRETS_FROM_VAULT
                value: '1'
              - name: VAULT_ADDR
                valueFrom:
                  secretKeyRef:
                    name: vault
                    key: address
              - name: VAULT_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: vault
                    key: token
              - name: PYPI_INDEX_URL
                valueFrom:
                  secretKeyRef:
                    name: pypi
                    key: address
              - name: APP_ENV
                value: production # TODO
          restartPolicy: OnFailure
          imagePullSecrets:
            - name: osuakatsuki-registry-secret
